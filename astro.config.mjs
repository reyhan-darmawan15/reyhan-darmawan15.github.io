import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://reyhan-darmawan15.github.io',
  // Uncomment base if deploying to a subdirectory on GitHub Pages:
  //base: '/dist',
});
